managed:
  redis: false

redis:
  enabled: true
  architecture: replication
  auth:
    password: "change-me"   # override in prod
  master:
    persistence:
      enabled: true
      size: 8Gi
  replica:
    replicaCount: 1
    persistence:
      enabled: true
      size: 8Gi
