// BROKEN — 'using' is not an index setting; 'default_expr' is not a column setting.
// Errors: "Unknown index setting 'using'" / "Unknown column setting 'default_expr'"
Table users {
  id int [pk]
  email varchar
  created_at timestamp [default_expr: `now()`]
  indexes { (email) [using: btree] }
}
